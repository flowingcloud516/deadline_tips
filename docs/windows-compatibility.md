# Windows 兼容性验证

## Windows 11 验证（2026-08-13）

- 环境：Windows 11 25H2，系统 Build 26200.8875，x64。
- WebView2：151.0.4129.78。
- 工具链：Node.js 24.15.0、Rust 1.97.1、Visual Studio Build Tools 2022 17.14.37、Windows 11 SDK 26100。
- 前端测试：41/41 通过。
- Rust 测试：6/6 通过，包含 JSON 原子写入、导出快照、非法 JSON 和不支持 schema 的导入拒绝。
- TypeScript 与 Vite 生产构建通过。
- Tauri release 编译通过。
- NSIS x64 安装包生成并静默安装成功，安装器退出码为 0。
- 安装后应用可以启动并保持响应；悬浮窗可见，管理窗口成功创建且默认隐藏。
- 窗口实测尺寸：悬浮窗约 495 × 439，管理窗口约 975 × 757（受系统 DPI 缩放影响）。
- 应用启动内存约 73.4 MiB，WebView2 用户数据目录正常创建。
- 最终安装包 SHA-256：`CAD770E0E807862683034F4281B72300FB6BA8AD78DE9406873E99C36B36F4BC`。

### 尚需人工确认

- 从任务管理页实际点击 Windows 文件对话框，目视确认导入、覆盖确认及导出提示文案和布局。
- 托盘菜单逐项点击、窗口拖动、多显示器和不同 DPI 下的位置恢复。
- 卸载后用户数据保留策略是否符合最终发布预期。

自动化和只读系统检查均已通过；以上项目依赖人工视觉或鼠标操作，不影响当前安装包的编译与启动结论。

## Windows 11 验证（0.2.0，2026-08-17）

- 前端测试：59/59 通过；Rust 测试：6/6 通过。
- TypeScript、Vite 和 Tauri release 构建通过，NSIS x64 安装包生成成功。
- NSIS 静默安装退出码为 0，卸载注册表显示版本 0.2.0。
- 启动安装后的程序，悬浮窗和管理窗口渲染正常，窗口继续保持不显示任务栏按钮。
- 真实点击悬浮窗“−”后窗口完全隐藏；通过托盘菜单“显示 / 隐藏悬浮窗”成功恢复。
- 真实点击进入任务管理窗口，将每日显示时间从 10:00 改为 10:05 并保存，JSON 持久化值正确；随后通过 UI 恢复为 10:00。
- 每日提醒的准点、错过补显示、每日去重和跨日重新调度由独立时间算法测试覆盖。
