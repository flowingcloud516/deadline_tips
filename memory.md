# Deadline Tips 项目记忆

## 协作约定

- 用户说“记录一下当前成果”时，默认执行：更新 `memory.md`、更新 `agenda.md`、提交 Git、推送当前分支到远端。
- 若发生合并冲突、仓库未配置远端或远端认证失败，应保留本地提交并明确说明阻塞项。
- Windows 10 是首要开发与回归环境；Windows 11 已完成首轮自动化、构建、安装和启动验证，仍需补齐人工交互验收。
- 桌面界面、图标和视觉资产应保持易于修改，并在关键阶段交由用户确认。

## 产品规则

- 悬浮窗分为近期任务、周期任务与重要任务三个互斥区域。
- 非重要周期任务全部显示在周期任务区域，不受近期天数范围限制。
- 周期任务的日期字段表示整体结束日期；下一次发生日期根据今天和重复规则动态计算，不能晚于结束日期。
- 周期任务卡片同时显示“下一次任务还有多少天”和“距整体截止还有多少天”。
- 悬浮窗和管理窗口按本地整点自动刷新日期，并在重新获得焦点或恢复可见时立即刷新，避免跨午夜后剩余天数停留在昨天。
- 重要任务只出现在重要任务区域，即使临期或逾期也不进入近期区域。
- 临期或逾期的重要任务在重要区域红色高亮。
- 重要任务显示剩余天数和约剩余周数。
- 第一版只支持 Windows 10/11、本地 JSON，不使用系统通知。
- 截止日期必须按本地日历计算，包含闰年 2 月场景。

## 当前实现

- 技术栈：Tauri 2、React、TypeScript、Vite、Zod、Vitest、Rust。
- 已实现任务领域模型、日期和周期规则、CRUD、筛选排序及重要任务分区。
- 已实现无边框悬浮窗、可靠拖动、缩放、位置尺寸恢复、双窗口和系统托盘。
- 悬浮窗和任务管理窗口均不显示 Windows 任务栏按钮，运行入口只保留可见界面与系统托盘图标。
- 已实现表格内新增、编辑弹窗、删除、原生日历日期控件及格式提示。
- 已实现本地 JSON 原子读写、数据路径显示、Windows 文件对话框选择及安全迁移。
- 已实现 JSON 导入/导出界面；导入会先校验并要求覆盖确认，不改变当前活动数据文件位置。
- 前端测试基线 53/53，Rust 测试基线 6/6。
- 已在 Windows 11 Build 26200.8875 完成 release 编译、NSIS 安装包生成、静默安装和启动响应验证。
- 最终验证安装包位于 `src-tauri/target/release/bundle/nsis/Deadline Tips_0.1.0_x64-setup.exe`；兼容性记录见 `docs/windows-compatibility.md`。
- 详细快照见 `docs/progress-2026-08-12.md`。

## 尚未完成

- 近期范围 3/7/14/30 天设置。
- 置顶开关与开机自启。
- 周期任务“完成本期”界面入口。
- 正式图标、发布文档、Win10 全量验收及 Win11 文件对话框、托盘、多显示器和多 DPI 人工验收。
